package com.realive.repository.cart;

// Querydsl 쿼리를 위한 커스텀 인터페이스
public interface CartItemRepositoryCustom {
    // 필요에 따라 Querydsl을 사용하는 메서드 정의
    // 현재 CartItemRepository의 JPA 메서드로 충분하므로, 특별한 Querydsl 메서드 정의는 생략합니다.
    // 하지만 복잡한 조인, 동적 쿼리 등이 필요하다면 여기에 정의합니다.
}